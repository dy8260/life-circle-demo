# ===== 百度地图开放平台「浏览器端」应用 AK（必填）=====
# 获取：https://lbsyun.baidu.com/apiconsole/key
# 创建「浏览器端」类型应用；Referer 白名单可填 localhost 或留空（开发用）
# 复制本文件为 .env 并填入真实 AK，再用 docker run --env-file .env 启动
BMAP_AK=

# ===== 容器对外端口（可选，仅作说明）=====
# 实际端口由 docker run -p 宿主端口:80 决定，例如 -p 8080:80
# 容器内 nginx 固定监听 80
