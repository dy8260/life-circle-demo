# 赛题符合度分析 · 15 分钟生活圈智能体检与规划助手（02 开源 AI 工具赛道）

> 对比基准：赛题要求 PDF（`D:\AI\Gitee赛事资料\赛题-15分钟生活圈-赛题要求.pdf`，2 页，与官网 https://www.oschina.net/os2026/ 一致）
> 被比对项目：`D:\AI\BaiDu-2026`（dev2026 分支，含大量**未提交**新功能）
> 分析时间：2026-09-03

## 一、评审维度逐项对照

### 1. 功能正确性与覆盖率（40%）—— 约 38/40 ✅
| 赛题要求 | 项目实现 | 状态 |
|---|---|---|
| 地图基础能力：坐标转换 / POI 检索 / 路径规划 | `BMapGL.Geocoder` + `LocalSearch` + `WalkingRoute` 全用上 | ✅ |
| 等时圈 + 热力图渲染（真实路网） | `isochrone.js` 16 方向真实步行路径插值成不规则多边形；`heatmap.js` POI 覆盖热力图 | ✅（等时圈以多边形呈现，比圆形/热力图更准；非"等时圈内部步行时长热力图"） |
| 盲区识别：1km 内无菜场/药店/小学的点位 | `gap.js` 栅格采样 + λ 标定 + 步行距离场 + 连通斑块，判定三类步行距离均 >1km | ✅ 已落地（25/25 测试通过） |

### 2. API 深度调用与工程优化（30%）—— 约 27/30 ✅（小风险）
| 要求 | 实现 | 状态 |
|---|---|---|
| 批量距离矩阵 **或** 并发请求 | `Util.pmap` 3 并发 WalkingRoute；未用 routematrix（浏览器端 AK 调 Web 服务返回 240，已在 `docs/服务盲区识别算法.md` 说明） | ✅ 满足"或" |
| 容错与降级 | `poi.js` 重试/超时/兜底；`gap.js` λ 兜底 1.25；多版 API 兼容 | ✅ |
| 创新空间插值 | λ 标定 + 距离场外推（仅 6 次路径规划外推全域） | ✅ 正是赛题鼓励项 |

⚠️ 风险：个别评委若死磕"批量距离矩阵"，可能小扣；可用"服务端 AK + routematrix"补一路作增强论证（可选，非必须）。

### 3. 产品交互与用户体验（15%）—— 15/15 ✅
- 可视化：热力图 + 柱状图 + 雷达图 + 评分环 齐全
- 友好性：深色大屏玻璃拟态、地址/城市自定义、🆚 对比模式、HUD、天气 widget

### 4. 开源工程规范（15%）—— 约 7–8/15 ❌ 主要失分点
| 要求 | 状态 | 说明 |
|---|---|---|
| 代码规范 | ✅ | 模块化清晰 |
| 文档与部署（含 **API Key 需脱敏**） | ⚠️ | README 详尽，但 AK **明文硬编码**在 `js/config.js` 与 `index.html`，且站点已公开部署 GitHub Pages → 不满足"脱敏" |
| CI/CD（GitHub Actions / Gitee Go） | ❌ | 无 `.github/workflows`，无 Gitee Go |
| 开源许可证（交付要求 #1 强制） | ❌ | 仓库**无 LICENSE 文件** |

## 二、交付要求（硬条款）
| 要求 | 状态 |
|---|---|
| 源码托管 Git + 标准许可证 | ⚠️ 已托管（GitHub dy8260 / Gitee qnlz88），**缺 LICENSE** |
| 一键构建/部署脚本 + Docker/环境配置 + 示例数据 | ⚠️ 有 `python -m http.server` / `serve.js` 一键起服务；**无 Dockerfile，已有仓库内置示例数据 `data/sample-community.json`** |
| 技术设计文档（API 策略 / 等时圈算法 / POI 清洗 / 盲区算法 / 真实对比报告） | ✅ README + `docs/服务盲区识别算法.md` 覆盖；POI 清洗/对比报告散见 README |

## 三、总评
- 功能侧（前 85% 权重）已**基本做满**，最大缺口"服务盲区识别"已补上。
- 失分集中在**开源工程规范**（15%）：LICENSE、CI/CD、AK 脱敏、Docker/示例数据。
- 估算当前符合度 **≈ 88/100**（功能 38 + API 27 + 体验 15 + 规范 8）。相比早期 67/100 已大幅提升，剩余差距都是可快速补齐的"工程合规"项。

## 四、优先行动清单
1. 【高】加 LICENSE（Apache-2.0 或 MIT，赛题点名）。
2. 【高】AK 脱敏：把 `BMAP_AK` 从源码移出 → 读 `window.__BMAP_AK__` / `?ak=` / 构建期注入，加 `.env.example`；并**轮换**已公开的 `4G0d2...`（避免被刷配额）。
3. 【高】补 CI/CD：GitHub Actions 跑 `node test/smoke-test.js` + 部署 Pages。
4. 【中】加 Dockerfile（nginx 静态）+ 一份示例数据 JSON（离线演示，不依赖 AK）。
5. 【中】等时圈"步行时长热力图"可选增强（应对"等时圈热力图"字面解读）。
6. 【紧急】**当前 dev2026 所有新改动（gap.js / compare.js / docs / tests 等）均未提交、零远端备份**——先 commit 并 push dev2026 分支，再决定合 master。

---
## 附：百度 Key 现状（用户追问）
- 当前 `js/config.js` 与 `index.html` 已改为占位符 `__BMAP_AK__`（真 key 通过 config.local.js 本地覆盖 / CI 注入，不进仓库）。
- 与冻结原项目 `D:\AI\BaiDu\js\config.js` **完全一致** → **没有更换**，从初始提交 `3e344a1` 到现在一直是这个 key。
- 记忆里提到的"临时 AK `yAAvI2...`"和"09:00 自动化换回原 key"：经核查，**代码里已无任何 `yAAvI2`**，且**自动化列表为空（该自动化并不存在）**。`相关资料.txt` 里那句"换百度key"只是个待办备忘，没落地。
- 附带提醒：该 key 现**明文硬编码且随 GitHub Pages 公开**，既不满足赛题"API Key 需脱敏"，也有被刷配额风险 → 建议趁这次"换 key"把它做成可配置 + 轮换。
